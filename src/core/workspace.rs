pub struct Workspace {
    // placeholder for future fields
}

impl Workspace {
    pub fn new() -> Self {
        Workspace {}
    }
}

impl Default for Workspace {
    fn default() -> Self {
        Self::new()
    }
}
